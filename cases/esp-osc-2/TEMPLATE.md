# node / none starter

This remains a draft and deliberately fails. Implement probe.mjs against the actual source, including a baseline negative control and candidate acceptance.

The none protocol example is illustrative, not an OSC/VISCA/SSC profile. Read the project from sources/esp-osc-2; the probe receives BCL_HOST/BCL_PORT for the loopback stand.

Default build recipe: npm run build. Adapt it to the project before marking ready. Python compileall checks syntax only. Node dependencies are not automatically installed: the existing audited dependency policy still applies. The Docker runtime already provides Node, Python and the C++ compiler.

Fill the passport's ownerCheck, repair and acceptance fields. Configure publication paths separately before generating publishable evidence.
