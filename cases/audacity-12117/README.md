# Collapse repeated Audacity project extensions

Issue: https://github.com/audacity/audacity/issues/12117

The baseline save path removes only the final suffix, so a Linux dialog result ending in `.aup4.aup4` remains duplicated. The candidate compiles and executes a standalone test of the production normalizer and registers focused project tests upstream.
