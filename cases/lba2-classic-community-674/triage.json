{
  "schemaVersion": 1,
  "checkedAt": "2026-09-14T08:53:29.726Z",
  "issue": {
    "repository": "LBALab/lba2-classic-community",
    "number": "674",
    "url": "https://github.com/LBALab/lba2-classic-community/issues/674",
    "title": "Android: a BIN/CUE rip in /sdcard/lba2cc is not discovered, though it plays when pointed at",
    "state": "open",
    "updatedAt": "2026-09-14T08:51:56Z"
  },
  "source": {
    "repository": "LBALab/lba2-classic-community",
    "requestedRef": null,
    "defaultBranch": "main",
    "archived": false,
    "license": "GPL-2.0",
    "commit": "069138f56515f7679bfb2b897bee16b1a77edfe4",
    "licenseMissing": false,
    "ruleFiles": [
      {
        "path": ".github/PULL_REQUEST_TEMPLATE.md",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/.github/PULL_REQUEST_TEMPLATE.md",
        "content": "<!--\nPR title format: `<type>(<scope>): <summary>`\nTypes: feat | fix | port | perf | refactor | docs | test | build | ci | chore\nScope is optional. Example: `fix(credits): preserve gameplay state across console-invoked credits`\nThe PR title drives the changelog whatever the merge mode. `main` takes merge commits by\ndefault, so your own commit messages land on it too and are worth writing.\nSee AGENTS.md \"Commit & PR conventions\".\n-->\n\n## What & why\n\n<!-- One or two sentences. What does this change and why does it matter? -->\n\n## Notes for reviewers\n\n<!-- Optional: tradeoffs considered, follow-ups, anything non-obvious. -->\n\n## Checklist\n\n- [ ] Build passes on my platform (Linux / macOS / Windows — pick one or more)\n- [ ] If LIB386 / 3DEXT touched: ASM equivalence tests pass (`./run_tests_docker.sh`)\n- [ ] If behavior changes: opt-in via flag/cvar/config (preserves default game feel)\n- [ ] Docs updated in the same PR if behavior or workflow changed\n- [ ] French comments and ASCII art preserved in any modified files\n",
        "requirementHints": [
          "Types: feat | fix | port | perf | refactor | docs | test | build | ci | chore",
          "The PR title drives the changelog whatever the merge mode. `main` takes merge commits by",
          "default, so your own commit messages land on it too and are worth writing.",
          "See AGENTS.md \"Commit & PR conventions\".",
          "- [ ] If LIB386 / 3DEXT touched: ASM equivalence tests pass (`./run_tests_docker.sh`)",
          "- [ ] French comments and ASCII art preserved in any modified files"
        ],
        "reviewStatus": "HUMAN_REVIEW_REQUIRED"
      },
      {
        "path": "AGENTS.md",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/AGENTS.md",
        "content": "# Agent guidance — LBA2 Classic Community\n\nThis document helps AI coding assistants (Cursor, Copilot, Claude, etc.) work effectively on this codebase. Read it before making changes or answering questions about the code.\n\n## Principles\n\n1. **Truth hierarchy and doc accuracy:** Code > project docs > external sources. Prefer `docs/GLOSSARY.md`, `docs/LIFECYCLES.md`, etc. over inference. When code and docs conflict, code wins — fix the docs in the same commit or PR (environment trumps willpower; maintainers may forget). Keep `docs/ASM_VALIDATION_PROGRESS.md` and `docs/ASM_TO_CPP_REFERENCE.md` current.\n\n2. **Preserve the nature of the game:** Do not change gameplay, feel, or behavior by default. Fix bugs that alter behavior; do not \"improve\" behavior without explicit agreement.\n\n3. **Optional features behind flags:** New behavior or features that change the experience must be opt-in (build options, config flags, runtime toggles). Default builds should match original behavior.\n\n4. **Quality over speed:** Equivalence tests must pass. Do not relax tests to unblock. Prefer smaller, well-tested changes. Fix bugs before merging.\n\n5. **Cross-platform priority:** Support Linux, macOS, and Windows (desktop) and Android (arm64-v8a / armeabi-v7a, mobile). Contributors and agents may be on any desktop host—do not assume Linux. Avoid platform-specific assumptions (paths, case sensitivity, toolchains). Prefer portable C/C++. No inline x86 in library code. The engine builds on SDL3, which supports [many more platforms](https://github.com/libsdl-org/SDL/blob/main/docs/README-platforms.md)—writing portable code today keeps that door open.\n\n## Never\n\n- Never relax equivalence tests or add approximate comparisons\n- Never point a user-facing string at the repo: no `docs/*.md`, no source paths, no issue numbers. Point at `README.txt` (it ships) or, better, say the thing itself. See [CODESTYLE.md \"Strings a player reads\"](CODESTYLE.md#strings-a-player-reads).\n- Never leak the session into a comment. Comments state the constraint or intent that outlives the merge, in the present tense, as if the code had always looked this way. No \"used to\", no \"the old code assumed\", no narrating the change you just made or the evidence that motivated it: that belongs in the commit message and the PR body, which is where a reader goes looking for it. Stripping the story must not strip the *why*, so keep the invariant, the hazard, or the non-obvious coupling the code can't show on its own. Bad: `/* a spelling that used to work would now be rejected */`. Good: `/* RES_DISCOVERY parses these with strcasecmp, so the validator must too, or it would reject a spelling that parser accepts. */`\n- Never remove French comments or ASCII art\n- Never add `__asm__` / inline x86 in LIB386 C++ files\n- Never change gameplay, timing, or feel without explicit approval\n- Never assume platform; never add unapproved dependencies\n- Never \"modernize\" C++98 to C++11+ in game code (tests may use newer)\n- Never add game assets (art, audio, models, textures) to the repo—they are not redistributable\n- Never hardcode API keys, credentials, or secrets\n- Never add GPL-incompatible code or dependencies; respect the project license (GPL v2). When copying code from elsewhere, ensure compatibility and proper attribution.\n- Never run clang-format on ASM, `LIB386/libsmacker/`, or other excluded files\n- Do only what was asked; avoid scope creep (refactoring, \"improving\" unrelated code)\n- When the user's intent is ambiguous, ask before proceeding—do not guess.\n\n## Golden rules\n\nOperational complements to the principles above.\n\n- **ASM is the source of truth.** All equivalence tests compare ASM vs C++ byte-for-byte. Use `ASSERT_ASM_CPP_EQ_INT` for scalars, `ASSERT_ASM_CPP_MEM_EQ` for buffers. See `docs/ASM_TEST_COVERAGE_AUDIT.md` for what \"covered\" means in practice (branches, side effects, edge inputs, ASM-invalid domains). This applies to ported engine code (LIB386, SOURCES/3DEXT). New SOURCES infrastructure with no ASM original (e.g. console, logging, control harness) is verified by host tests, not ASM equivalence.\n- **Think like an archaeologist:** This codebase is a window into 1990s game dev at Adeline Software. Surface historical context when relevant. See `docs/FRENCH_COMMENTS.md` and `docs/ASCII_ART.md`.\n- **Search before adding:** Check for existing implementations before adding new code; avoid duplication.\n- **Pin fixes with a test or a repro hook.** When fixing a bug, first ask whether the affected logic is pure-data (parsing, serialization, math, projection, sort) — if yes, extracting it into a pure function and adding a host test is usually a small additional refactor and should be done as part of the fix. When automation is impractical (state, timing, UI), ship a manual repro hook instead (a console command, debug menu entry, or build flag). See [CONTRIBUTING.md \"Doing good work here\"](CONTRIBUTING.md#doing-good-work-here) for the rationale.\n\n## Execution style (Karpathy-inspired)\n\nApply these behavior rules on every non-trivial task:\n\n1. **Think before coding:** State assumptions explicitly. If intent is ambiguous, ask before implementing. Surface tradeoffs instead of silently picking one path.\n2. **Simplicity first:** Prefer the minimum code and the minimum API needed to satisfy the request. Avoid speculative abstractions and \"future-proofing\" unless requested.\n3. **Surgical changes:** Touch only files and lines needed for the requested outcome. Avoid drive-by refactors, formatting churn, or unrelated cleanup.\n4. **Goal-driven execution:** Define concrete success criteria (preferably tests or reproducible checks), implement, then verify results before declaring done.\n\n## Project overview\n\n- **What:** Community fork of the LBA2 (Twinsen's Odyssey) engine for preservation and modernization.\n- **Main work:** Porting x86 assembly to C++ while preserving exact behavior. ASM remains the source of truth.\n- **Stack:** C++98 game logic, LIB386 engine libraries (C linkage), x86 ASM (UASM), SDL3, libsmacker.\n- **Structure:** `SOURCES/` = game logic; `LIB386/` = engine (3D, audio, rendering); `docs/` = reference documentation.\n\n## Build & test\n\n- **Build:** `cmake -B build && cmake --build build`. Or use presets for your platform: `cmake --preset <preset> && cmake --build --preset <preset>` — `linux`, `macos_arm64`, `macos_x86_64`, `windows_ucrt64` (see `CMakePresets.json`).\n- **Game data:** Retail files are not in the repo. See `docs/GAME_DATA.md`. Set `LBA2_GAME_DIR`, use `--game-dir`, or place data in `./data` / `../LBA2` relative to the repo. From anywhere in the clone: `./scripts/dev/build-and-run.sh` or `make run` (repo root).\n- **Tests:** Run via Docker: `./run_tests_docker.sh`. Works on Linux, macOS (Docker + QEMU), and Windows (Docker Desktop). Tests require 32-bit x86 + UASM inside the container.\n- **Host tests:** `make test` (or `ctest -L host_quick` after configure with `-DLBA2_BUILD_TESTS=ON -DLBA2_BUILD_ASM_EQUIV_TESTS=OFF`). No Docker, no retail files. CI runs these on Linux, macOS, and Windows (see `.github/workflows/*.yml`).\n- **Filter:** `./run_tests_docker.sh test_getang2d test_lirot3df`\n- **Bisect:** `./run_tests_docker.sh --bisect` to find first divergent draw call\n- **Before considering done:** Run `./run_tests_docker.sh` (or N/A if docs-only). If modifying formatted files: run `bash ./scripts/ci/apply-format.sh` (or enable the pre-commit hook — see \"Code conventions\"); CI also runs the format check.\n- **Also before considering done, if the change adds a global, a header include, or a platform conditional:** run `make arch-check`. It holds the code to boundaries this file and CODESTYLE.md already state, and prints the sentence behind whichever one fails. CI runs it on every push.\n- **When tests fail:** Do not relax tests. For ASM↔CPP: use `--bisect` to find first divergence, add debug traces to both ASM and CPP, extract failing inputs into a focused unit test, fix CPP to match ASM. For other bugs: read the relevant subsystem doc (AUDIO, MENU, DEBUG, etc.), map the code path, fix the bug, verify with build/tests.\n- **Minimal build** (no audio/video): `-DSOUND_BACKEND=null -DMVIDEO_BACKEND=null` for quick iteration.\n- **Switch render resolution at launch:** `--resolution WxH` (e.g. `--resolution 768x480`, `--resolution 1280x720`). Range `320x200` – `1920x1080`, width must be a multiple of 8. Default (no flag) is the compile-time `RESOLUTION_X/Y` (640x480). Useful for sweeping width-dependent bugs from one binary instead of rebuilding per width (see `docs/WIDESCREEN_HARDCODED_DIMS_AUDIT.md`). *Dev-mode for now*: the engine renders correctly, but the 2D UI is still anchored at 640x480 (Tier 2 work).\n- **Drive it programmatically:** the CLI control harness boots, restores a save, runs console commands, advances N ticks, writes a JSON state snapshot / PNG, and exits, all in one invocation: `lba2cc --headless --no-autosave --load <save> --tick 30 --dump-state s.json --screenshot s.png --exit`. Use it to verify a runtime change without clicking through the game: compare `--dump-state` before/after, or diff a screenshot. Add `--fixed-dt 16` for byte-reproducible dumps so you can assert exact rather than eyeball. Needs retail data; `tests/automation/run.sh` runs the suite. Modal commands (`give <item>`, `playvideo`) hang a `--exit` run, see caveats. Run `lba2cc --help-all` for the full flag list (plain `--help` is the player-facing subset). See [docs/CONTROL.md](docs/CONTROL.md).\n- **Always pass `--headless` (and usually `--no-autosave`).** Without it the engine opens a window, and **the game pauses when that window loses focus**, so a run that loses focus gets a stopped clock and runs in parallel fight over it: six identical `--fixed-dt` runs windowed produced five different sim states, headless they were byte-identical. `--fixed-dt` only buys you determinism inside `--headless`. `--no-autosave` stops the run rewriting `autosave.lba` on your own install (explicit saves, including `savebug`, still work). Two more traps worth knowing: `--exec` fires on the first tick and *races* a `cube` change (which lands the next frame), so use `--exec-at <tick> \"<cmds>\"` when a command depends on the new scene; and the first frame after `--load` is a full redraw, which hides exactly the partial-frame rendering bugs (stale background, dirty-box) the harness is good at catching.\n- **Drive game state, not just saves:** for state- or quest-gated interactions the harness can't reach by input (no movement/menu), use `teleport` / `teleport actor <n>` (position the hero), `varcube` / `vargame <n> [value]` (read or set the scene/game Life variables that gate quest progression), and `lifetrace <objN>` (log an NPC's Life script: comportement/track/zone + every `LF_` condition it evaluates). One `--exec` line can then reproduce an interaction (position + quest flags + script observability) instead of hunting for the exact playthrough save. See [docs/CONSOLE.md](docs/CONSOLE.md).\n\n## When modifying X, do Y\n\n| Modifying | Do | See |\n|-----------|-----|-----|\n| LIB386 C++ port | Check ASM_VALIDATION_PROGRESS Notes; add/update equivalence test | docs/ASM_VALIDATION_PROGRESS.md |\n| SOURCES (game logic) | No ASM tests; use docs/GLOSSARY, LIFECYCLES for domain terms | docs/GLOSSARY.md, docs/LIFECYCLES.md |\n| SOURCES/3DEXT/ | Same as LIB386; check ASM_TO_CPP_REFERENCE | docs/ASM_TO_CPP_REFERENCE.md |\n| Adding ASM↔CPP test | Use `add_asm_cpp_test()`, include stress test, update `docs/ASM_VALIDATION_PROGRESS.md`; check `docs/ASM_TEST_COVERAGE_AUDIT.md` for the coverage rubric | docs/TESTING.md, docs/ASM_TEST_COVERAGE_AUDIT.md, .github/copilot-instructions.md |\n| Audio/video | AIL in LIB386/AIL/; backends SDL, Miles, null | docs/AUDIO.md |\n| Android-specific behaviour (JNI, TV detection, storage permission) | Put it behind `LIB386/SYSTEM/ANDROID.{CPP,H}` with a stub-on-desktop; `<jni.h>` in that TU only. The two layer rules this is a case of, callers stay `#ifdef`-free and LIB386 never includes `SOURCES/`, are in CODESTYLE.md \"Layers\" | CODESTYLE.md, docs/ANDROID.md, docs/PLATFORM.md §8 |\n| Adding a platform conditional, a global, an STL include, or a `DEFINES.H` include | `make arch-check` refuses all four outside the layer that owns them. A file that genuinely belongs in the platform layer joins a named list in the checker, and owes a label in the per-module table | scripts/ci/check-arch.py, docs/plan/ARCH_RULES_PLAN.md, docs/ENGINE_GAME_SEAM.md |\n| Debug tools | DEBUG_TOOLS (console is always available) | docs/DEBUG.md, docs/CONSOLE.md |\n| Verifying a runtime change (scene, hero, inventory, render) | Drive the engine non-interactively and assert on dumped state / a screenshot instead of manual play | docs/CONTROL.md |\n| Config / lba2.cfg | Keys, persistence, installer vs game, embedded default. The config beside the game data is read as a layer under the run's own, not copied into it; writes reach the owned file alone | docs/CONFIG.md, docs/GAME_DATA.md |\n| Persisting one setting from code | `WriteConfigValue` (PERSO.H), not `DefFileBufferWriteValue`. A bare write is refused while the game data's config is layered under the player's, which is every install that ships one, and a caller that does not check the return reports the setting applied while storing nothing | docs/CONFIG.md |\n| Running the engine at all | Pass `--user-dir <tmp>` so the run writes to a folder you own. Without it the run reads and writes the developer's real saves and settings, and a `--language` or a settings change persists there. `--profile <name>` nests inside it; `LBA2_USER_DIR` is the same for a wrapper. Comparing against a build that predates the flag needs `XDG_DATA_HOME`, which is the only lever both honour | docs/GAME_DATA.md, docs/plan/PORTABILITY_PLAN.md |\n| Paths, discovery, the writable side | Run `scripts/dev/dist_check.sh` before and after: six assertions across every install you own, non-zero exit. It caught a live defect the day it was written | docs/TESTING.md §6b |\n| Anything named `*Version*` | Five unrelated fields share the name; check which one before touching. `DistribVersion` (cfg `Version`) is release identity, `NumVersion` is save layout, `Version_US` is dead | docs/VERSIONS.md, docs/SAVEGAME.md |\n| Adding a log or diagnostic line | Prefer `Log_*` (`<SYSTEM/LOG.H>`) at the true severity for committed code; `Log_Debug` is off by default (all sinks), surfacing only at `--log-level`/`loglevel debug`. Throwaway `printf` is fine; don't commit it | \"Logging\" below, LIB386/H/SYSTEM/LOG.H |\n| Code that reads retail HQR data or legacy save formats | Check the rule: never `sizeof(T)`-as-stride for fat structs; use a paired `T_DISK` or field-by-field serialization | docs/ABI.md |\n| File with French comments or ASCII art | Preserve; add new comments alongside | docs/FRENCH_COMMENTS.md, docs/ASCII_ART.md |\n| New subsystem or reference doc | Create docs/<name>.md; add to docs/README.md; update in same commit | docs/README.md |\n| Writing a plan, design trail, or research writeup before the work | Put it in `docs/plan/`, not `docs/`. Add it to the \"Plans & research\" table in docs/README.md with a status. `docs/` is what the engine does today; `docs/plan/` is what was proposed and why. When a plan lands, mark it Implemented and fold the durable parts into the reference doc; a plan must never be the only description of shipped behaviour | docs/README.md \"Plans & research\" |\n| Adding, moving, or removing a script under scripts/ | Add or update its row in scripts/README.md in the same commit | scripts/README.md |\n| Making a script or workflow depend on a new external tool | Add its row to docs/TOOLING.md **and** a probe to `scripts/dev/check-tooling.sh` in the same commit. Never restate a version in the doc — cite the file that pins it | docs/TOOLING.md |\n| Any code that affects documented behavior | Update the doc in the same commit | Principle 1 |\n| A subsystem the knowledge bundle has a concept for: timing, recording, control, console, transitions, save, input, movement | Read the owning Subsystem under `docs/knowledge/` and its Quirks before changing behaviour that looks wrong; a Quirk is original behaviour that is load bearing and looks like a defect. A change to a contract a concept carries updates the concept in the same PR, and a new load-bearing fact is a new concept rather than a comment. `make knowledge-check` is the gate | docs/knowledge/index.md, docs/knowledge/SCHEMA.md |\n| Fixing a bug | If the affected logic is pure-data, extract it to a pure function and add a host test. Otherwise add a manual repro hook (console command, debug flag) | CONTRIBUTING.md \"Doing good work here\" |\n| Editing docs (any `.md`) | Sentence-case headings; bold for structure only (list-item / paragraph leads, table row labels), not mid-sentence emphasis; verify file/line refs; from `docs/`, link to source with `../SOURCES/...` / `../LIB386/...` | \"Editing docs\" below; CONTRIBUTING.md \"Doing good work here\" |\n\n## Code conventions\n\nStyle rules and rationale (language dialect, indentation, types, C++98, preservation) live in [CODESTYLE.md](CODESTYLE.md) — read it before writing engine code. The agent-critical invariant: **ported original code stays C-style to mirror the ASM; new infrastructure with no ASM original may use conservative C++98 features.** Never modernize game code (see the Never list). When unsure, match the file you are editing.\n\n**Adding a new subsystem: give it its own `.CPP`, and let it own its globals.** Do not grow an original file with code that never existed in 1997, and do not declare a new module's state in `C_EXTERN.H` / `GLOBAL.CPP`. The header declares exactly what the `.CPP` defines, so callers state their dependency in their include list. In a *surface* (cfg, console, CLI, saves, state dumps: generic files that serve every feature) that rule inverts. A surface must not name a feature's variables; it offers a hook and the feature fills it in, as `Foo_ReadConfig` / `Foo_WriteConfig` / `Foo_RegisterCvars`. [SOURCES/CONTROL.CPP](SOURCES/CONTROL.CPP) is the same rule from the other side: it owns the per-run overrides and exposes `Control_HasX` / `Control_GetX` for consumers to ask. Full rule and rationale in [CODESTYLE.md \"Where new code goes\"](CODESTYLE.md#where-new-code-goes); [SOURCES/FOLLOWCAM.CPP](SOURCES/FOLLOWCAM.CPP) is the worked example. This matters because the dialect rule above is undecidable inside a file that is half original and half new.\n\n**Proving a refactor that should not change behaviour.** Relocation work is worth verifying by comparison, not by argument, and the engine's traces make that cheap: build the base commit and the change, run both under a pinned clock and identical input, and diff the trace. For camera work that is `--fixed-dt 16 --exec \"camtrace 1\" --exec-at <n> \"camnudge ...\" --tick <n> --exit`, diffed on the `[cam]` lines; other subsystems have their own (`objtrace`, `lifetrace`, `--dump-state`, `--projrec`). A byte-identical trace is a stronger result than any test asserting the same thing, and it costs one rebuild.\n\n**Resolving an asset path: never hand-roll filename spellings.** Retail files are uppercase while the engine asks in lower case, and the only thing bridging that is `Discover` in [SOURCES/DIRECTORIES.CPP](SOURCES/DIRECTORIES.CPP), which probes as-is, upper, lower and title case. Build every asset path through `GetResPath` / `GetJinglePath` / `GetMoviePath` / `GetVoicePath` so it inherits those probes. Writing your own `snprintf` of a candidate name works on the install you tested and silently misses on another: `STREAM.CPP`'s `track<N>.ogg` fallback hand-rolls two spellings and loses every theme track on an all-uppercase install while the jingles keep playing, which reads as \"some music is missing\" rather than a filename problem.\n\nFormatting mechanics (agent-operational):\n\n- Formatting: clang-format with checked-in `.clang-format`; exclusions live in `.clang-format-ignore` (ASM, `LIB386/libsmacker/`, vendored `stb_*`, and a handful of legacy lookup-table files). When adding vendored third-party code or generated/hand-tuned lookup tables, add the path to `.clang-format-ignore` in the same commit — single source of truth for local scripts, CI, and the pre-commit hook.\n- clang-format version: pinned once in `scripts/ci/clang-format-select.sh` (`CLANG_FORMAT_MAJOR`, currently 18). Install that exact version (`clang-format-18`) so local formatting matches CI; the scripts refuse to run a different major rather than silently disagreeing with CI.\n- Pre-commit hook (optional, recommended): `git config core.hooksPath scripts/git-hooks` once per clone. It runs clang-format `--dry-run` on staged C/C++ files (respecting `.clang-format-ignore`) and blocks commits with violations. Fix with `bash ./scripts/ci/apply-format.sh && git add -u`. Bypass with `git commit --no-verify` or `SKIP_FORMAT=1` when warranted.\n\n## Logging\n\nOne pipe: every log call fans out to `adeline.log` (always), stderr, and the in-engine console. API in [LIB386/H/SYSTEM/LOG.H](LIB386/H/SYSTEM/LOG.H); design and history in [docs/LOGGING_UNIFICATION.md](docs/LOGGING_UNIFICATION.md). This is about ergonomics, not ceremony; it isn't meant to police throwaway debugging.\n\n- **Pick the true severity.** `Log_Info` / `Log_Warn` / `Log_Error`, plus `Log_Raw` for verbatim lines (no severity tag).\n- **One master level gates every sink.** A single global level (default `INFO`) is checked before any sink, so it controls `adeline.log`, the terminal, and the console together. Set it with `--log-level <debug|info|warn|error>`, the `LBA2_LOG_LEVEL` env var, or the `loglevel` console command (`Log_SetLevel` in code). Structural framing (the identity banner and section markers, `Log_Banner` / `Log_BeginSection`) bypasses the level so a pasted log stays self-describing even when raised; `Log_Raw` verbatim lines obey the level like normal records.\n- **`Log_Debug` is for low-volume, meaningful detail** (a save written, a scene/cube change, an init result), not per-frame or per-object output. Off by default; it surfaces everywhere at once (`adeline.log`, terminal, console) when the level is lowered to `debug` (`--log-level debug` / `loglevel debug`). The test: someone who turns debug on should get signal, not a wall of text. If a line can fire many times a second it does not belong here (see the trace bullet). Varargs like `printf`, so there's no friction reason to prefer `printf` in committed code.\n- **Throwaway debugging is your business** — a quick `printf` while iterating is fine; just don't commit it.\n- **Spammy per-frame / per-subsystem tracing is a runtime toggle, not a severity.** This is the \"trace\" tier: high-volume diagnostics you switch on from the console only while chasing a specific problem (the `audio` and `video log` switches are the pattern; `sfxLogEnabled` / `musicLogEnabled` guard the call sites, silent by default). There is deliberately no `TRACE` log level: such traces are usually temporary and pulled once the bug is found, so a persistent severity would be YAGNI. Don't route this kind of chatter through `Log_Debug`.\n- **No log categories/channels** (deliberately — YAGNI). To filter by area, prefix the message text (`[SFX]`, `[CD]`) and grep the file.\n- `LogPrintf` / `LogPuts` are legacy shims onto the same fan-out — fine where they are; no need to add new calls.\n- **The stderr sink always emits, so headless runs are visible.** On a real TTY it's ANSI-coloured; when redirected (harness, CI, `2>file`) it falls back to plain, severity-tagged lines with the same info and no escape codes. So a piped `--load ... --tick ... --exit` run shows the boot log and any `Log_*` output inline on stderr; you don't have to open `adeline.log`. For a debug trace in the harness, add `--log-level debug`. `stdout` stays a pure data channel (`--dump-state`, `--exec` mirror), never logs.\n- **Before the sinks exist** (early boot), `Log_*` falls back to stderr so errors still surface.\n\n## Editing docs\n\nFor tone and engineering principles, see [CONTRIBUTING.md \"Doing good work here\"](CONTRIBUTING.md#doing-good-work-here). The rules below are the doc-formatting standard on top of that.\n\n**User-facing vs agent-facing.** README, CONTRIBUTING, CHANGELOG, and most of `docs/` are user-facing — tone matters and the rules below apply. AGENTS.md and `.github/copilot-instructions.md` are agent-facing — accuracy and structure for agents to parse matter most; tone and bold density matter less.\n\n**Formatting:**\n\n- Headings: sentence case. Proper nouns, place names, and acronyms keep their capitals (e.g. \"MSYS2\", \"ASM\", \"Citadel Island\", \"GitHub release\").\n- Bold for structure, not emphasis. Keep list-item leads (`- **Foo:** description`), paragraph leads (`**Note:**`, `**Why:**`), and table row labels. Strip mid-sentence emphasis on inline code, product names, and short noun phrases.\n- Italics for semantic markers (`*why*`, `*what*`) are fine. Don't italicise for decoration.\n- Leave smart quotes (`\"…\"`) and `--` (double-hyphen) dashes alone — don't normalise.\n- Reflow paragraph hard-wraps for consistency: prefer one paragraph per line and one item per list-row. If a doc mixes wrapped and unwrapped paragraphs, reflow.\n\n**Accuracy (always pair with formatting):**\n\n- **Run `make docs-symbols` after editing a doc that names code, and `make docs-links` for references.** The first checks that a symbol a doc says is in a file is actually defined there, which a link checker cannot: when code moves the path still resolves and the prose still names the old file. Both run in CI (`docs-links.yml`). `make docs-links` needs `lychee` on PATH and skips its link half silently without it, still exiting 0, so confirm its summary line printed rather than trusting the exit code.\n- **Run `make knowledge-check` after editing anything under `docs/knowledge/`.** It is the bundle's lint, it runs in CI on every change, and it blocks. Every concept needs `type`, `title`, `description` and `generated`, a Quirk, Decision or Format needs one `owner`, and each type has required body sections; relation and body links resolve inside the bundle. An agent never writes a `human:` actor, and `docs/knowledge/porting/*.md` is regenerated with `scripts/dev/knowledge_porting.py` and never edited. `make knowledge-drift` lists concepts whose cited files changed since their `as_of`.\n- **In the bundle, a footnote cites the file as a relative link and the routine in backticks after it.** `make docs-symbols` checks each routine against the file linked nearest before it, and `make knowledge-graph` turns the same links into edges in `graphify-out/graph.json` after a `graphify update .`, so `graphify explain \"Routine()\"` lists the concepts that guard a routine; a bare path is a citation neither can read.\n- **Prefer naming the enclosing function to citing a line number.** Line numbers rot on the next edit to the file, and nothing checks them; a function name is stable and greppable, and `make docs-symbols` verifies it.\n- Retail asset files are **UPPERCASE**: `LBA2.HQR`, `RESS.HQR`, `TEXT.HQR`, and so on, in every distribution (Steam, both GOG builds, the retail disc). Name them that way in docs, in user-facing strings, and in shell commands a reader will paste. Lower case is easy to type and names a file nobody has, which sends a stuck player renaming files instead of fixing the real problem. Two deliberate exceptions, both kept lower case on purpose: the token list in [docs/RELEASING.md](docs/RELEASING.md) that exists to show grep false positives, and LBA1 markers in [docs/plan/LBA1_PORT_PLAN.md](docs/plan/LBA1_PORT_PLAN.md).\n- Quoting a C identifier or string literal is different from naming a file. `HQR_NAMES.H` spells its constants lower case (`RESS_HQR_NAME \"ress.hqr\"`), and a doc quoting that source should match the source. Upper case is for \"this file is on your disk\"; the literal's own case is for \"this is what the code says\".\n- Directory names have no single truth, so leave them alone: Steam ships `Music/` but `VIDEO/` and `VOX/`, and other installs differ. Case discovery handles it.\n- Verify file paths, line numbers, function names, and command flags against the working tree before believing them.\n- From `docs/`, links to `SOURCES/` or `LIB386/` need the `../` prefix (`[…](../SOURCES/X.CPP)`). Common breakage spot. One level deeper (`docs/plan/`, `docs/widescreen/`) it is `../../SOURCES/...`, and a sibling doc back up in `docs/` is `../CONTROL.md`. `make docs-links` checks these, plus `#anchors` and the `docs/<name>.md` paths named in source comments; CI runs it on every change.\n- Cross-check claims about CMake options, presets, and CI workflows against `CMakeLists.txt`, `CMakePresets.json`, and `.github/workflows/`.\n\n**Special cases:**\n\n- Verbatim attributed external content (e.g. [docs/COMPILER_NOTES.md](docs/COMPILER_NOTES.md)): leave entirely as-is.\n- Curated content (`docs/FRENCH_COMMENTS.md`, `docs/ASCII_ART.md`, `docs/ASM_VALIDATION_PROGRESS.md`): light touch only — H1 + the project's own framing prose.\n\n## Commit & PR conventions\n\nThe **PR title** drives the changelog. `git-cliff` parses it from the\ncommit log on `main` regardless of which merge mode you pick (merge,\nsquash, or rebase), so the title has to follow\n[Conventional Commits](https://www.conventionalcommits.org/):\n\n```\n<type>(<optional scope>): <summary>\n```\n\n**`main` takes merge commits by default.** A branch's own commits land on\n`main` and stay there, so write each one for a reader who arrives at it from\n`git blame` with no memory of the PR. Squash is the exception rather than the\nrule: reach for it when a branch's individual commits are not worth keeping\n(fixup churn, \"address review\" churn), and it collapses them into the PR title.\n\nAllowed types:\n\n| Type       | Use for                                                     |\n|------------|-------------------------------------------------------------|\n| `feat`     | New feature visible to players or contributors              |\n| `fix`      | Bug fix                                                     |\n| `port`     | ASM → C++ port work (fork-specific, distinct from refactor) |\n| `perf`     | Performance improvement, no behavior change                 |\n| `refactor` | Code restructuring, no behavior change                      |\n| `docs`     | Documentation only                                          |\n| `test`     | Adding or tightening tests                                  |\n| `build`    | Build system, CMake, presets                                |\n| `ci`       | CI workflow plumbing (matrix, runners, labels). For user-facing CI work like a release pipeline that ships binaries, prefer `feat:` so it lands in **Added** where end-users look. |\n| `chore`    | Housekeeping — **skipped from the public changelog**        |\n\nScope is optional but useful (`fix(credits): ...`, `port(SORT): ...`).\n\nA lightweight CI check (`.github/workflows/pr-title.yml`) verifies the PR\ntitle format. **Individual commits inside the PR are not format-checked:**\nonly the PR title is enforced. That is about enforcement, not about care:\nunder the default merge mode those commits are what `main` keeps.\nContributors do not need to edit `CHANGELOG.md`;\nit is regenerated at release time. Use `chore:` for any PR you want kept\nout of the public changelog (formatting, tooling, internal cleanups).\nSee [docs/RELEASING.md](docs/RELEASING.md).\n\n**Do not include `(#issue_num)` in the PR title.** When a PR is\nsquash-merged, GitHub appends `(#PR_num)` to the resulting commit subject\n— if the title already contained `(#issue_num)`, the commit ends up with\nboth: e.g. `fix(credits): ... (#65) (#66)`. Reference issues in the PR\n*body* instead, using GitHub's standard keywords (`Closes #65`,\n`Fixes #65`, `Refs #65`).\n\n## Further reading\n\n- [LICENSE](LICENSE) — GPL v2; project license\n- [CODESTYLE.md](CODESTYLE.md) — Canonical code-style rules: language dialect by zone, indentation, types, C++98, preservation\n- [CONTRIBUTING.md](CONTRIBUTING.md) — Contribution workflow, formatter mechanics, PR workflow\n- [.github/copilot-instructions.md](.github/copilot-instructions.md) — Detailed ASM↔CPP workflow, polyrec debugging, test patterns, common pitfalls\n- [docs/FEATURE_WORKFLOW.md](docs/FEATURE_WORKFLOW.md) — Reasoning and docs for big features (console, headless, menu, camera)\n- [docs/BUG_HUNTING.md](docs/BUG_HUNTING.md): finding defects nobody reported, with socket-driven fuzzing under sanitizers and the oracle checks that make a green result mean something\n- [docs/README.md](docs/README.md) — Full documentation index\n- [docs/knowledge/index.md](docs/knowledge/index.md): the knowledge bundle, what is true about the engine and why, one concept per file; [SCHEMA.md](docs/knowledge/SCHEMA.md) is its house profile and [subsystems/knowledge.md](docs/knowledge/subsystems/knowledge.md) its charter, read before adding or generating a concept\n- [scripts/README.md](scripts/README.md) — Catalogue of developer, CI, and packaging scripts (maintained vs. spike)\n- [docs/TOOLING.md](docs/TOOLING.md) — External tools the repo expects, tiered by what breaks without them; `scripts/dev/check-tooling.sh` probes them\n",
        "requirementHints": [
          "This document helps AI coding assistants (Cursor, Copilot, Claude, etc.) work effectively on this codebase. Read it before making changes or answering questions about the code.",
          "1. **Truth hierarchy and doc accuracy:** Code > project docs > external sources. Prefer `docs/GLOSSARY.md`, `docs/LIFECYCLES.md`, etc. over inference. When code and docs conflict, code wins — fix the docs in the same commit or PR (environment trumps willpower; maintainers may forget). Keep `docs/ASM_VALIDATION_PROGRESS.md` and `docs/ASM_TO_CPP_REFERENCE.md` current.",
          "3. **Optional features behind flags:** New behavior or features that change the experience must be opt-in (build options, config flags, runtime toggles). Default builds should match original behavior.",
          "4. **Quality over speed:** Equivalence tests must pass. Do not relax tests to unblock. Prefer smaller, well-tested changes. Fix bugs before merging.",
          "- Never relax equivalence tests or add approximate comparisons",
          "- Never leak the session into a comment. Comments state the constraint or intent that outlives the merge, in the present tense, as if the code had always looked this way. No \"used to\", no \"the old code assumed\", no narrating the change you just made or the evidence that motivated it: that belongs in the commit message and the PR body, which is where a reader goes looking for it. Stripping the story must not strip the *why*, so keep the invariant, the hazard, or the non-obvious coupling the code can't show on its own. Bad: `/* a spelling that used to work would now be rejected */`. Good: `/* RES_DISCOVERY parses these with strcasecmp, so the validator must too, or it would reject a spelling that parser accepts. */`",
          "- Never remove French comments or ASCII art",
          "- Never \"modernize\" C++98 to C++11+ in game code (tests may use newer)",
          "- Never hardcode API keys, credentials, or secrets",
          "- When the user's intent is ambiguous, ask before proceeding—do not guess.",
          "- **ASM is the source of truth.** All equivalence tests compare ASM vs C++ byte-for-byte. Use `ASSERT_ASM_CPP_EQ_INT` for scalars, `ASSERT_ASM_CPP_MEM_EQ` for buffers. See `docs/ASM_TEST_COVERAGE_AUDIT.md` for what \"covered\" means in practice (branches, side effects, edge inputs, ASM-invalid domains). This applies to ported engine code (LIB386, SOURCES/3DEXT). New SOURCES infrastructure with no ASM original (e.g. console, logging, control harness) is verified by host tests, not ASM equivalence.",
          "- **Think like an archaeologist:** This codebase is a window into 1990s game dev at Adeline Software. Surface historical context when relevant. See `docs/FRENCH_COMMENTS.md` and `docs/ASCII_ART.md`.",
          "- **Search before adding:** Check for existing implementations before adding new code; avoid duplication.",
          "- **Pin fixes with a test or a repro hook.** When fixing a bug, first ask whether the affected logic is pure-data (parsing, serialization, math, projection, sort) — if yes, extracting it into a pure function and adding a host test is usually a small additional refactor and should be done as part of the fix. When automation is impractical (state, timing, UI), ship a manual repro hook instead (a console command, debug menu entry, or build flag). See [CONTRIBUTING.md \"Doing good work here\"](CONTRIBUTING.md#doing-good-work-here) for the rationale.",
          "1. **Think before coding:** State assumptions explicitly. If intent is ambiguous, ask before implementing. Surface tradeoffs instead of silently picking one path.",
          "4. **Goal-driven execution:** Define concrete success criteria (preferably tests or reproducible checks), implement, then verify results before declaring done.",
          "## Build & test",
          "- **Tests:** Run via Docker: `./run_tests_docker.sh`. Works on Linux, macOS (Docker + QEMU), and Windows (Docker Desktop). Tests require 32-bit x86 + UASM inside the container.",
          "- **Host tests:** `make test` (or `ctest -L host_quick` after configure with `-DLBA2_BUILD_TESTS=ON -DLBA2_BUILD_ASM_EQUIV_TESTS=OFF`). No Docker, no retail files. CI runs these on Linux, macOS, and Windows (see `.github/workflows/*.yml`).",
          "- **Filter:** `./run_tests_docker.sh test_getang2d test_lirot3df`",
          "- **Bisect:** `./run_tests_docker.sh --bisect` to find first divergent draw call",
          "- **Before considering done:** Run `./run_tests_docker.sh` (or N/A if docs-only). If modifying formatted files: run `bash ./scripts/ci/apply-format.sh` (or enable the pre-commit hook — see \"Code conventions\"); CI also runs the format check.",
          "- **Also before considering done, if the change adds a global, a header include, or a platform conditional:** run `make arch-check`. It holds the code to boundaries this file and CODESTYLE.md already state, and prints the sentence behind whichever one fails. CI runs it on every push.",
          "- **When tests fail:** Do not relax tests. For ASM↔CPP: use `--bisect` to find first divergence, add debug traces to both ASM and CPP, extract failing inputs into a focused unit test, fix CPP to match ASM. For other bugs: read the relevant subsystem doc (AUDIO, MENU, DEBUG, etc.), map the code path, fix the bug, verify with build/tests.",
          "- **Switch render resolution at launch:** `--resolution WxH` (e.g. `--resolution 768x480`, `--resolution 1280x720`). Range `320x200` – `1920x1080`, width must be a multiple of 8. Default (no flag) is the compile-time `RESOLUTION_X/Y` (640x480). Useful for sweeping width-dependent bugs from one binary instead of rebuilding per width (see `docs/WIDESCREEN_HARDCODED_DIMS_AUDIT.md`). *Dev-mode for now*: the engine renders correctly, but the 2D UI is still anchored at 640x480 (Tier 2 work).",
          "- **Drive it programmatically:** the CLI control harness boots, restores a save, runs console commands, advances N ticks, writes a JSON state snapshot / PNG, and exits, all in one invocation: `lba2cc --headless --no-autosave --load <save> --tick 30 --dump-state s.json --screenshot s.png --exit`. Use it to verify a runtime change without clicking through the game: compare `--dump-state` before/after, or diff a screenshot. Add `--fixed-dt 16` for byte-reproducible dumps so you can assert exact rather than eyeball. Needs retail data; `tests/automation/run.sh` runs the suite. Modal commands (`give <item>`, `playvideo`) hang a `--exit` run, see caveats. Run `lba2cc --help-all` for the full flag list (plain `--help` is the player-facing subset). See [docs/CONTROL.md](docs/CONTROL.md).",
          "| LIB386 C++ port | Check ASM_VALIDATION_PROGRESS Notes; add/update equivalence test | docs/ASM_VALIDATION_PROGRESS.md |",
          "| SOURCES (game logic) | No ASM tests; use docs/GLOSSARY, LIFECYCLES for domain terms | docs/GLOSSARY.md, docs/LIFECYCLES.md |",
          "| Adding ASM↔CPP test | Use `add_asm_cpp_test()`, include stress test, update `docs/ASM_VALIDATION_PROGRESS.md`; check `docs/ASM_TEST_COVERAGE_AUDIT.md` for the coverage rubric | docs/TESTING.md, docs/ASM_TEST_COVERAGE_AUDIT.md, .github/copilot-instructions.md |",
          "| Paths, discovery, the writable side | Run `scripts/dev/dist_check.sh` before and after: six assertions across every install you own, non-zero exit. It caught a live defect the day it was written | docs/TESTING.md §6b |",
          "| Anything named `*Version*` | Five unrelated fields share the name; check which one before touching. `DistribVersion` (cfg `Version`) is release identity, `NumVersion` is save layout, `Version_US` is dead | docs/VERSIONS.md, docs/SAVEGAME.md |",
          "| Adding a log or diagnostic line | Prefer `Log_*` (`<SYSTEM/LOG.H>`) at the true severity for committed code; `Log_Debug` is off by default (all sinks), surfacing only at `--log-level`/`loglevel debug`. Throwaway `printf` is fine; don't commit it | \"Logging\" below, LIB386/H/SYSTEM/LOG.H |",
          "| File with French comments or ASCII art | Preserve; add new comments alongside | docs/FRENCH_COMMENTS.md, docs/ASCII_ART.md |",
          "| New subsystem or reference doc | Create docs/<name>.md; add to docs/README.md; update in same commit | docs/README.md |",
          "| Writing a plan, design trail, or research writeup before the work | Put it in `docs/plan/`, not `docs/`. Add it to the \"Plans & research\" table in docs/README.md with a status. `docs/` is what the engine does today; `docs/plan/` is what was proposed and why. When a plan lands, mark it Implemented and fold the durable parts into the reference doc; a plan must never be the only description of shipped behaviour | docs/README.md \"Plans & research\" |",
          "| Adding, moving, or removing a script under scripts/ | Add or update its row in scripts/README.md in the same commit | scripts/README.md |",
          "| Making a script or workflow depend on a new external tool | Add its row to docs/TOOLING.md **and** a probe to `scripts/dev/check-tooling.sh` in the same commit. Never restate a version in the doc — cite the file that pins it | docs/TOOLING.md |",
          "| Any code that affects documented behavior | Update the doc in the same commit | Principle 1 |",
          "| A subsystem the knowledge bundle has a concept for: timing, recording, control, console, transitions, save, input, movement | Read the owning Subsystem under `docs/knowledge/` and its Quirks before changing behaviour that looks wrong; a Quirk is original behaviour that is load bearing and looks like a defect. A change to a contract a concept carries updates the concept in the same PR, and a new load-bearing fact is a new concept rather than a comment. `make knowledge-check` is the gate | docs/knowledge/index.md, docs/knowledge/SCHEMA.md |",
          "| Fixing a bug | If the affected logic is pure-data, extract it to a pure function and add a host test. Otherwise add a manual repro hook (console command, debug flag) | CONTRIBUTING.md \"Doing good work here\" |",
          "Style rules and rationale (language dialect, indentation, types, C++98, preservation) live in [CODESTYLE.md](CODESTYLE.md) — read it before writing engine code. The agent-critical invariant: **ported original code stays C-style to mirror the ASM; new infrastructure with no ASM original may use conservative C++98 features.** Never modernize game code (see the Never list). When unsure, match the file you are editing.",
          "**Adding a new subsystem: give it its own `.CPP`, and let it own its globals.** Do not grow an original file with code that never existed in 1997, and do not declare a new module's state in `C_EXTERN.H` / `GLOBAL.CPP`. The header declares exactly what the `.CPP` defines, so callers state their dependency in their include list. In a *surface* (cfg, console, CLI, saves, state dumps: generic files that serve every feature) that rule inverts. A surface must not name a feature's variables; it offers a hook and the feature fills it in, as `Foo_ReadConfig` / `Foo_WriteConfig` / `Foo_RegisterCvars`. [SOURCES/CONTROL.CPP](SOURCES/CONTROL.CPP) is the same rule from the other side: it owns the per-run overrides and exposes `Control_HasX` / `Control_GetX` for consumers to ask. Full rule and rationale in [CODESTYLE.md \"Where new code goes\"](CODESTYLE.md#where-new-code-goes); [SOURCES/FOLLOWCAM.CPP](SOURCES/FOLLOWCAM.CPP) is the worked example. This matters because the dialect rule above is undecidable inside a file that is half original and half new.",
          "**Proving a refactor that should not change behaviour.** Relocation work is worth verifying by comparison, not by argument, and the engine's traces make that cheap: build the base commit and the change, run both under a pinned clock and identical input, and diff the trace. For camera work that is `--fixed-dt 16 --exec \"camtrace 1\" --exec-at <n> \"camnudge ...\" --tick <n> --exit`, diffed on the `[cam]` lines; other subsystems have their own (`objtrace`, `lifetrace`, `--dump-state`, `--projrec`). A byte-identical trace is a stronger result than any test asserting the same thing, and it costs one rebuild.",
          "**Resolving an asset path: never hand-roll filename spellings.** Retail files are uppercase while the engine asks in lower case, and the only thing bridging that is `Discover` in [SOURCES/DIRECTORIES.CPP](SOURCES/DIRECTORIES.CPP), which probes as-is, upper, lower and title case. Build every asset path through `GetResPath` / `GetJinglePath` / `GetMoviePath` / `GetVoicePath` so it inherits those probes. Writing your own `snprintf` of a candidate name works on the install you tested and silently misses on another: `STREAM.CPP`'s `track<N>.ogg` fallback hand-rolls two spellings and loses every theme track on an all-uppercase install while the jingles keep playing, which reads as \"some music is missing\" rather than a filename problem.",
          "- Formatting: clang-format with checked-in `.clang-format`; exclusions live in `.clang-format-ignore` (ASM, `LIB386/libsmacker/`, vendored `stb_*`, and a handful of legacy lookup-table files). When adding vendored third-party code or generated/hand-tuned lookup tables, add the path to `.clang-format-ignore` in the same commit — single source of truth for local scripts, CI, and the pre-commit hook.",
          "- Pre-commit hook (optional, recommended): `git config core.hooksPath scripts/git-hooks` once per clone. It runs clang-format `--dry-run` on staged C/C++ files (respecting `.clang-format-ignore`) and blocks commits with violations. Fix with `bash ./scripts/ci/apply-format.sh && git add -u`. Bypass with `git commit --no-verify` or `SKIP_FORMAT=1` when warranted.",
          "- **One master level gates every sink.** A single global level (default `INFO`) is checked before any sink, so it controls `adeline.log`, the terminal, and the console together. Set it with `--log-level <debug|info|warn|error>`, the `LBA2_LOG_LEVEL` env var, or the `loglevel` console command (`Log_SetLevel` in code). Structural framing (the identity banner and section markers, `Log_Banner` / `Log_BeginSection`) bypasses the level so a pasted log stays self-describing even when raised; `Log_Raw` verbatim lines obey the level like normal records.",
          "- **`Log_Debug` is for low-volume, meaningful detail** (a save written, a scene/cube change, an init result), not per-frame or per-object output. Off by default; it surfaces everywhere at once (`adeline.log`, terminal, console) when the level is lowered to `debug` (`--log-level debug` / `loglevel debug`). The test: someone who turns debug on should get signal, not a wall of text. If a line can fire many times a second it does not belong here (see the trace bullet). Varargs like `printf`, so there's no friction reason to prefer `printf` in committed code.",
          "- **Throwaway debugging is your business** — a quick `printf` while iterating is fine; just don't commit it.",
          "- **Before the sinks exist** (early boot), `Log_*` falls back to stderr so errors still surface."
        ],
        "reviewStatus": "HUMAN_REVIEW_REQUIRED"
      },
      {
        "path": "CONTRIBUTING.md",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/CONTRIBUTING.md",
        "content": "# How to contribute\n\nThis is a community project. Maintainers and contributors come and go as life allows — that's fine and expected. The goal is a sustainable, friendly place to work on an old game we love. Pick something that interests you, move at your own pace, and ship it when it's ready.\n\nA few notes that keep it sustainable:\n\n- **Keep PRs focused.** One topic per PR. Sweeping rewrites or whole-subsystem changes (e.g. \"port everything to Rust\", reorganizing a whole subdirectory) deserve a heads-up on [Discord](https://discord.gg/jsTPWYXHsh) or an issue *before* you start — reviewer time is the scarce resource, not code.\n- **AI assistants are welcome** (Cursor, Copilot, Claude, etc.). The bar is the same as for any contributor: the code has to be correct, the scope has to match what the PR title claims, and ASM equivalence still has to hold. Point your assistant at [AGENTS.md](AGENTS.md) for project context, principles, and the \"never\" list.\n- **Ask early when in doubt.** A short question on [Discord](https://discord.gg/jsTPWYXHsh) beats a week of work in the wrong direction.\n\n## Doing good work here\n\nA few principles. The *why* matters more than the rule — once you understand the why, you can apply judgment in cases the rule doesn't cover. None of this is unique to this project; it's how good engineering works generally. It's written down because this is a community project where reviewer time is scarce, and these habits are what keep quality high *without* anyone having to police it.\n\n- **Own what you ship.** If you used an AI assistant, you are still the author. Read the diff yourself before opening the PR; you should be able to explain every line and defend every decision under review. *Why:* outsourcing typing is fine — outsourcing *understanding* is how subtle bugs land in old code. Your name on the PR is a commitment.\n\n- **Actually test your change.** Build it. Run the game (or the relevant test). Walk through the path your change touches. Then try one path it doesn't touch, in case you broke something next door. *Why:* \"the automated tests pass\" is necessary, not sufficient. This codebase is 25 years old and full of failure modes nothing has ever automated. You are the last pair of eyes before review.\n\n- **Write tests when you reasonably can.** Especially for new logic, regressions you just fixed, and anything in `LIB386/` where ASM equivalence is the bar. *Why:* a test you write today catches the bug your future self introduces in six months, on a platform you don't own, for a contributor you've never met. That's a great trade.\n\n- **CI is on your side.** When it fails, read the log and fix the cause. Don't disable a check, skip a hook (`--no-verify`), or work around the failure to \"unblock\" yourself. *Why:* CI catches what you forgot to check on your machine — that's its whole job, and it's free. Fighting it just defers the same problem to whoever merges after you, often on a platform you didn't test.\n\n- **Explain *why*, not *what*.** In PR descriptions, commit messages, and code comments alike. *Why:* the diff already shows what changed. What it can't show is why this approach over the obvious alternative, what constraint you hit, or what you considered and rejected. That's the part future contributors (and your future self) will need.\n\n- **Smaller is better.** The smallest change that solves the problem is easier to review, easier to revert if it turns out to be wrong, and easier to come back to after a break. *Why:* every line is a maintenance cost. Resist \"while I'm here\" cleanups; open a separate PR instead.\n\n- **Ship a test, or ship a repro hook.** Bugs in pure-data code paths (parsing, serialization, math, projection, sort) are almost always cheap to test once you extract the affected logic into a pure function — that small refactor is usually worth doing as part of the fix, with a host test pinning it. See `tests/3D/test_sintab.cpp` and the savegame corpus harness as patterns. When automation genuinely isn't realistic (state machines, input timing, UI flow), at least ship a manual repro hook alongside the fix: a console command, a debug menu entry, or a build flag that re-triggers the affected path. PR #66's `credits [0|1]` console command is the canonical example. *Why:* future you (or someone else) will debug this same area again. A test pins it forever; a repro hook saves an hour of figuring out how to re-trigger the bug.\n\n- **Editing docs.** Match the project's tone (lean, direct, no AI tells) and the formatting standard in [AGENTS.md \"Editing docs\"](AGENTS.md#editing-docs) — sentence-case headings, bold for structure not emphasis, smart quotes / `--` dashes hands-off, and from `docs/` link to source with `../` (`../../` from `docs/plan/`). A doc describing what the engine does today goes in [docs/](docs/); a plan or research writeup for work not yet done goes in [docs/plan/](docs/plan/). Either way, add it to [docs/README.md](docs/README.md) in the same commit.\n\n## Reporting crashes and bugs\n\nIf you observe a bug you can report it [here](https://github.com/LBALab/lba2-classic-community/issues/new). When possible include a save (*.lba) — it makes reproducing the issue much faster.\n\n## Proposing features\n\nIf you have an idea, you can [create a feature request](https://github.com/LBALab/lba2-classic-community/issues/new) to suggest a new feature.\n\n## Contributing code\n\n1. Fork the repository\n2. Create a feature branch from `main`\n3. Make your changes\n4. Run `make test` before submitting for a quick host pass (or N/A if docs-only), then run `./run_tests_docker.sh` before merge when code changes affect behavior. CI will catch failures, but local runs save round-trips. PRs also run host tests on Linux, macOS, and Windows plus format; full ASM equivalence is the Docker workflow on Linux.\n5. Submit a pull request\n\nPRs and pushes run build checks on Linux, Windows (MSYS2 UCRT64), and macOS (arm64); see `.github/workflows/`. Equivalence tests still run in Docker on Linux only (`test.yml`).\n\n### PR titles\n\nYour PR title becomes the changelog entry for the PR. Format it as `<type>(<scope>): <summary>` — for example `fix(credits): preserve gameplay state across console-invoked credits`. A CI check verifies the format. The full type list and rationale live in [AGENTS.md](AGENTS.md#commit--pr-conventions); `chore:` is the explicit \"skip this from the public changelog\" prefix for housekeeping PRs. Individual commits inside your PR can be free-form — only the PR title is enforced. You do not need to edit `CHANGELOG.md`; it's regenerated at release time.\n\n### Setting up a development environment\n\nSee the [README](README.md) for prerequisites and build instructions. In short:\n\n```bash\ncmake -B build\ncmake --build build\n```\n\nTo find out what your machine is missing before you hit it, run\n`./scripts/dev/check-tooling.sh` (or `make check-tooling`). It probes every\nexternal tool the repo uses and exits non-zero only when the clone genuinely\ncannot build; the full tiered index is in [docs/TOOLING.md](docs/TOOLING.md).\n\nThe default build includes SDL3-based audio and Smacker FMV playback. Override with `-DSOUND_BACKEND=null -DMVIDEO_BACKEND=null` for a minimal build; see the build options table in the README.\n\nTo run the game you need retail data (not in the repo); see [docs/GAME_DATA.md](docs/GAME_DATA.md).\n\n### Code style\n\nThe style rules and the reasoning behind them — language dialect (C-in-`.cpp` by zone), indentation, types, the C++98 standard, and preservation of original code — live in [CODESTYLE.md](CODESTYLE.md). Read it before writing code, and keep changes consistent with the style of the file you are modifying. The rest of this section covers the *mechanics* of running the formatter.\n\nFor C and C++ files, the repository now uses `clang-format` with a checked-in style file. In VS Code, workspace settings enable format-on-save for C and C++ when the recommended `ms-vscode.cpptools` extension is installed.\n\nFor local formatting work, use the repo scripts:\n\n```bash\nbash ./scripts/ci/check-format.sh\nbash ./scripts/ci/apply-format.sh\n```\n\n`check-format.sh` verifies the tracked C/C++ files that are enforced in CI. `apply-format.sh` applies that same formatter policy to tracked clean files in your worktree.\n\nTo catch format issues before they reach CI, enable the optional pre-commit hook once per clone:\n\n```bash\ngit config core.hooksPath scripts/git-hooks\n```\n\nThe hook runs `clang-format --dry-run` against staged C/C++ files only (it checks the staged blob, so unrelated unstaged edits and partial-stage workflows still work) and reuses `.clang-format-ignore`, so it stays in sync with CI. When it blocks a commit, run `bash ./scripts/ci/apply-format.sh && git add -u` and try again. Bypass with `git commit --no-verify`, or `SKIP_FORMAT=1` for a session, when you genuinely need to.\n\nIf you use VS Code, install the workspace recommendations from `.vscode/extensions.json`. The current recommended set is `ms-vscode.cpptools`, `ms-vscode.cmake-tools`, and `EditorConfig.EditorConfig`.\n\n`EditorConfig.EditorConfig` is recommended because this repository also has files that are not auto-formatted by `clang-format`. It makes VS Code honor the checked-in `.editorconfig` for baseline indentation rules, which keeps C/C++ files on 4 spaces and preserves tab-based indentation in ASM and related files.\n\nASM files are intentionally excluded from automatic formatting, and `LIB386/libsmacker/` is kept out of formatting because it is third-party code.\n\nA small set of preservation-sensitive or macro-heavy files is also excluded from automatic formatting where `clang-format` does not produce stable results yet. Keep those files manual until they are split up or annotated for safer tooling.\n\nThe enforced exclusion list lives in `.clang-format-ignore`, and the CI/local scripts under `scripts/ci/` read that file directly.\n\nIf you are doing the one-time whitespace migration or reviewing it locally, keep it as a dedicated formatting-only commit and then configure git blame to ignore that commit:\n\n```bash\ngit config blame.ignoreRevsFile .git-blame-ignore-revs\n```\n\nAfter the bootstrap formatting commit exists, add its SHA to `.git-blame-ignore-revs`. Do not mix semantic changes into that commit.\n\n### Preservation of original code\n\nThis project values preservation: original French comments and ASCII art banners stay, clarifying comments go alongside the originals rather than replacing them, and history/culture content is attributed to the original codebase (lba2-classic). The full rules are in [CODESTYLE.md \"Preservation of original code\"](CODESTYLE.md#preservation-of-original-code).\n\n## Contributing to the documentation\n\nYou can also help build the official documentation here: https://github.com/LBALab/lba-classic-doc\n",
        "requirementHints": [
          "- **Keep PRs focused.** One topic per PR. Sweeping rewrites or whole-subsystem changes (e.g. \"port everything to Rust\", reorganizing a whole subdirectory) deserve a heads-up on [Discord](https://discord.gg/jsTPWYXHsh) or an issue *before* you start — reviewer time is the scarce resource, not code.",
          "- **Own what you ship.** If you used an AI assistant, you are still the author. Read the diff yourself before opening the PR; you should be able to explain every line and defend every decision under review. *Why:* outsourcing typing is fine — outsourcing *understanding* is how subtle bugs land in old code. Your name on the PR is a commitment.",
          "- **Actually test your change.** Build it. Run the game (or the relevant test). Walk through the path your change touches. Then try one path it doesn't touch, in case you broke something next door. *Why:* \"the automated tests pass\" is necessary, not sufficient. This codebase is 25 years old and full of failure modes nothing has ever automated. You are the last pair of eyes before review.",
          "- **Write tests when you reasonably can.** Especially for new logic, regressions you just fixed, and anything in `LIB386/` where ASM equivalence is the bar. *Why:* a test you write today catches the bug your future self introduces in six months, on a platform you don't own, for a contributor you've never met. That's a great trade.",
          "- **CI is on your side.** When it fails, read the log and fix the cause. Don't disable a check, skip a hook (`--no-verify`), or work around the failure to \"unblock\" yourself. *Why:* CI catches what you forgot to check on your machine — that's its whole job, and it's free. Fighting it just defers the same problem to whoever merges after you, often on a platform you didn't test.",
          "- **Explain *why*, not *what*.** In PR descriptions, commit messages, and code comments alike. *Why:* the diff already shows what changed. What it can't show is why this approach over the obvious alternative, what constraint you hit, or what you considered and rejected. That's the part future contributors (and your future self) will need.",
          "- **Ship a test, or ship a repro hook.** Bugs in pure-data code paths (parsing, serialization, math, projection, sort) are almost always cheap to test once you extract the affected logic into a pure function — that small refactor is usually worth doing as part of the fix, with a host test pinning it. See `tests/3D/test_sintab.cpp` and the savegame corpus harness as patterns. When automation genuinely isn't realistic (state machines, input timing, UI flow), at least ship a manual repro hook alongside the fix: a console command, a debug menu entry, or a build flag that re-triggers the affected path. PR #66's `credits [0|1]` console command is the canonical example. *Why:* future you (or someone else) will debug this same area again. A test pins it forever; a repro hook saves an hour of figuring out how to re-trigger the bug.",
          "- **Editing docs.** Match the project's tone (lean, direct, no AI tells) and the formatting standard in [AGENTS.md \"Editing docs\"](AGENTS.md#editing-docs) — sentence-case headings, bold for structure not emphasis, smart quotes / `--` dashes hands-off, and from `docs/` link to source with `../` (`../../` from `docs/plan/`). A doc describing what the engine does today goes in [docs/](docs/); a plan or research writeup for work not yet done goes in [docs/plan/](docs/plan/). Either way, add it to [docs/README.md](docs/README.md) in the same commit.",
          "4. Run `make test` before submitting for a quick host pass (or N/A if docs-only), then run `./run_tests_docker.sh` before merge when code changes affect behavior. CI will catch failures, but local runs save round-trips. PRs also run host tests on Linux, macOS, and Windows plus format; full ASM equivalence is the Docker workflow on Linux.",
          "5. Submit a pull request",
          "PRs and pushes run build checks on Linux, Windows (MSYS2 UCRT64), and macOS (arm64); see `.github/workflows/`. Equivalence tests still run in Docker on Linux only (`test.yml`).",
          "Your PR title becomes the changelog entry for the PR. Format it as `<type>(<scope>): <summary>` — for example `fix(credits): preserve gameplay state across console-invoked credits`. A CI check verifies the format. The full type list and rationale live in [AGENTS.md](AGENTS.md#commit--pr-conventions); `chore:` is the explicit \"skip this from the public changelog\" prefix for housekeeping PRs. Individual commits inside your PR can be free-form — only the PR title is enforced. You do not need to edit `CHANGELOG.md`; it's regenerated at release time.",
          "To find out what your machine is missing before you hit it, run",
          "The style rules and the reasoning behind them — language dialect (C-in-`.cpp` by zone), indentation, types, the C++98 standard, and preservation of original code — live in [CODESTYLE.md](CODESTYLE.md). Read it before writing code, and keep changes consistent with the style of the file you are modifying. The rest of this section covers the *mechanics* of running the formatter.",
          "To catch format issues before they reach CI, enable the optional pre-commit hook once per clone:",
          "The hook runs `clang-format --dry-run` against staged C/C++ files only (it checks the staged blob, so unrelated unstaged edits and partial-stage workflows still work) and reuses `.clang-format-ignore`, so it stays in sync with CI. When it blocks a commit, run `bash ./scripts/ci/apply-format.sh && git add -u` and try again. Bypass with `git commit --no-verify`, or `SKIP_FORMAT=1` for a session, when you genuinely need to.",
          "If you are doing the one-time whitespace migration or reviewing it locally, keep it as a dedicated formatting-only commit and then configure git blame to ignore that commit:",
          "After the bootstrap formatting commit exists, add its SHA to `.git-blame-ignore-revs`. Do not mix semantic changes into that commit.",
          "This project values preservation: original French comments and ASCII art banners stay, clarifying comments go alongside the originals rather than replacing them, and history/culture content is attributed to the original codebase (lba2-classic). The full rules are in [CODESTYLE.md \"Preservation of original code\"](CODESTYLE.md#preservation-of-original-code)."
        ],
        "reviewStatus": "HUMAN_REVIEW_REQUIRED"
      }
    ],
    "codeExamples": [
      "LIB386/3D/CAMERA.CPP",
      "LIB386/3D/COPYMATF.CPP",
      "LIB386/3D/DATAMAT.CPP",
      "LIB386/3D/DISTANCE.CPP",
      "LIB386/3D/GETANG2D.CPP",
      "LIB386/3D/GETANG3D.CPP",
      "LIB386/3D/IMATSTD.CPP",
      "LIB386/3D/IMATSTDF.CPP",
      "LIB386/3D/IMATTRAF.CPP",
      "LIB386/3D/LIGHT.CPP"
    ],
    "buildFiles": [
      {
        "path": "CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/CMakeLists.txt"
      },
      {
        "path": "LIB386/3D/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/LIB386/3D/CMakeLists.txt"
      },
      {
        "path": "LIB386/AIL/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/LIB386/AIL/CMakeLists.txt"
      },
      {
        "path": "LIB386/AIL/MILES/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/LIB386/AIL/MILES/CMakeLists.txt"
      },
      {
        "path": "LIB386/AIL/NULL/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/LIB386/AIL/NULL/CMakeLists.txt"
      },
      {
        "path": "LIB386/AIL/SDL/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/LIB386/AIL/SDL/CMakeLists.txt"
      },
      {
        "path": "LIB386/ANIM/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/LIB386/ANIM/CMakeLists.txt"
      },
      {
        "path": "LIB386/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/LIB386/CMakeLists.txt"
      },
      {
        "path": "LIB386/FILEIO/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/LIB386/FILEIO/CMakeLists.txt"
      },
      {
        "path": "LIB386/MENU/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/LIB386/MENU/CMakeLists.txt"
      },
      {
        "path": "LIB386/OBJECT/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/LIB386/OBJECT/CMakeLists.txt"
      },
      {
        "path": "LIB386/SNAPSHOT/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/LIB386/SNAPSHOT/CMakeLists.txt"
      },
      {
        "path": "LIB386/SVGA/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/LIB386/SVGA/CMakeLists.txt"
      },
      {
        "path": "LIB386/SYSTEM/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/LIB386/SYSTEM/CMakeLists.txt"
      },
      {
        "path": "LIB386/VIDEO_AUDIO_RESAMPLE/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/LIB386/VIDEO_AUDIO_RESAMPLE/CMakeLists.txt"
      },
      {
        "path": "LIB386/libsmacker/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/LIB386/libsmacker/CMakeLists.txt"
      },
      {
        "path": "LIB386/pol_work/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/LIB386/pol_work/CMakeLists.txt"
      },
      {
        "path": "Makefile",
        "kind": "make",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/Makefile"
      },
      {
        "path": "SOURCES/3DEXT/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/SOURCES/3DEXT/CMakeLists.txt"
      },
      {
        "path": "SOURCES/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/SOURCES/CMakeLists.txt"
      },
      {
        "path": "SOURCES/CONSOLE/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/SOURCES/CONSOLE/CMakeLists.txt"
      },
      {
        "path": "tests/3D/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/tests/3D/CMakeLists.txt"
      },
      {
        "path": "tests/3DEXT/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/tests/3DEXT/CMakeLists.txt"
      },
      {
        "path": "tests/ANIM/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/tests/ANIM/CMakeLists.txt"
      },
      {
        "path": "tests/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/tests/CMakeLists.txt"
      },
      {
        "path": "tests/MENU/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/tests/MENU/CMakeLists.txt"
      },
      {
        "path": "tests/OBJECT/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/tests/OBJECT/CMakeLists.txt"
      },
      {
        "path": "tests/SNAPSHOT/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/tests/SNAPSHOT/CMakeLists.txt"
      },
      {
        "path": "tests/SVGA/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/tests/SVGA/CMakeLists.txt"
      },
      {
        "path": "tests/SYSTEM/CMakeLists.txt",
        "kind": "cpp-cmake",
        "url": "https://github.com/LBALab/lba2-classic-community/blob/069138f56515f7679bfb2b897bee16b1a77edfe4/tests/SYSTEM/CMakeLists.txt"
      }
    ]
  },
  "findings": [
    {
      "code": "ACTIVE_RELATED_PR",
      "severity": "info",
      "detail": "A related PR is active. Review its scope: help validate it or identify a distinct repair; do not assume it already solves this issue. Resolved for this revision: The only related PR is this campaign's already-reviewed candidate, so tracking it does not compete with another contributor.",
      "url": "https://github.com/LBALab/lba2-classic-community/issues/674",
      "resolution": {
        "reason": "The only related PR is this campaign's already-reviewed candidate, so tracking it does not compete with another contributor.",
        "evidence": [
          "https://github.com/LBALab/lba2-classic-community/pull/675"
        ]
      }
    }
  ],
  "relatedPRs": [
    {
      "url": "https://github.com/LBALab/lba2-classic-community/pull/675",
      "relation": "open PR explicitly mentions issue",
      "title": "fix(android): discover disc images in app data folder",
      "author": "iibaranov-IG",
      "state": "open",
      "draft": true,
      "merged": false,
      "headSha": "1beede02afcb82016be7eed094df3e4485ddfc31",
      "updatedAt": "2026-09-14T08:51:45Z"
    }
  ],
  "dependencies": [],
  "errors": [],
  "scope": {
    "cloned": false,
    "built": false,
    "reproduced": false,
    "maxPages": 5,
    "maxRelatedPRs": 20
  },
  "coverage": {
    "sourceTree": "complete",
    "issueTimeline": "complete",
    "openPRs:LBALab/lba2-classic-community": "complete"
  },
  "decision": "READY_TO_INVESTIGATE",
  "nextActions": [
    "Review the reported behavior and build instructions; define the baseline failure before cloning or scheduling a build."
  ],
  "limitations": [
    "READY_TO_INVESTIGATE means metadata is sufficient to begin engineering review, not a reproduced or repaired bug.",
    "Dependency manifests are scanned heuristically; transitive dependencies, availability of registry packages, native libraries and physical equipment are not verified.",
    "Only explicit PR references are matched; semantically related work without links can be missed.",
    "Runtime compatibility, contribution policy and complete reproduction steps still require engineering review."
  ],
  "ranking": {
    "exclusions": [],
    "unknownGates": [],
    "assessmentBoundToRevision": true,
    "eligible": true,
    "score": 91,
    "scoreRange": [
      91,
      92
    ],
    "dimensions": [
      {
        "key": "ownerPain",
        "weight": 12,
        "value": 5,
        "reason": "First launch cannot use a valid rip without adb intervention.",
        "evidence": [
          "https://github.com/LBALab/lba2-classic-community/issues/674"
        ]
      },
      {
        "key": "freshness",
        "weight": 6,
        "value": 5,
        "reason": "Issue was reported today.",
        "evidence": [
          "https://github.com/LBALab/lba2-classic-community/issues/674"
        ]
      },
      {
        "key": "ownerAvailability",
        "weight": 12,
        "value": 5,
        "reason": "Reporter supplied exact logs, revision and reproduction details.",
        "evidence": [
          "https://github.com/LBALab/lba2-classic-community/issues/674"
        ]
      },
      {
        "key": "resultClarity",
        "weight": 10,
        "value": 5,
        "reason": "Success is a boot with Assets and Disc log lines from the documented directory.",
        "evidence": [
          "https://github.com/LBALab/lba2-classic-community/issues/674"
        ]
      },
      {
        "key": "reproducibility",
        "weight": 12,
        "value": 4,
        "reason": "The source branch and host discovery tests are deterministic; final device path needs Android.",
        "evidence": [
          "https://github.com/LBALab/lba2-classic-community/pull/675"
        ]
      },
      {
        "key": "repairSize",
        "weight": 7,
        "value": 5,
        "reason": "The repair changes two existing probe calls and their documentation.",
        "evidence": [
          "https://github.com/LBALab/lba2-classic-community/pull/675/files"
        ]
      },
      {
        "key": "verificationCost",
        "weight": 7,
        "value": 4,
        "reason": "Host suite is cheap; one reporter device check remains.",
        "evidence": [
          "https://github.com/LBALab/lba2-classic-community/pull/675"
        ]
      },
      {
        "key": "regressionRisk",
        "weight": 8,
        "value": 5,
        "reason": "Only two app-specific roots gain image scanning; broad storage roots stay unchanged.",
        "evidence": [
          "https://github.com/LBALab/lba2-classic-community/pull/675/files"
        ]
      },
      {
        "key": "projectActivity",
        "weight": 7,
        "value": 5,
        "reason": "Main and issue tracker are active.",
        "evidence": [
          "https://github.com/LBALab/lba2-classic-community/commits/main"
        ]
      },
      {
        "key": "communityValue",
        "weight": 7,
        "value": 4,
        "reason": "The fix helps Android owners using legal CD rips.",
        "evidence": [
          "https://github.com/LBALab/lba2-classic-community/issues/674"
        ]
      },
      {
        "key": "reuse",
        "weight": 6,
        "value": 4,
        "reason": "It exercises BCL's C++ discovery and external-owner validation path.",
        "evidence": [
          "https://github.com/LBALab/lba2-classic-community/pull/675"
        ]
      },
      {
        "key": "cascade",
        "weight": 6,
        "value": 3,
        "reason": "The pattern applies to documented app-specific storage probes.",
        "evidence": [
          "https://github.com/LBALab/lba2-classic-community/pull/675/files"
        ]
      }
    ],
    "class": "TAKE_NOW",
    "deliveryReadiness": 95,
    "deliveryProbability": null,
    "deliveryNote": "Readiness is an evidence-based human assessment, not a calibrated probability. Unknown inputs are never scored as facts.",
    "queueKey": [
      5,
      95,
      91
    ],
    "selectable": true
  },
  "durationMs": 10471
}
