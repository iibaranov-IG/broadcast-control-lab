# Camera Gallery Card #237

The visual editor excluded `camera.*` entities in `unknown` or `unavailable`
state even though YAML accepted them.

The BCL scenario applies the regression test to the pinned upstream revision,
records the expected failure, applies the production repair, reruns the same
test green, and executes the repository's complete `npm run check` gate.
Physical Home Assistant rendering remains an owner check.
