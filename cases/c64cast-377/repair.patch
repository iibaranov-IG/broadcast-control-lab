diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index 08673fb..18a50f8 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -92,7 +92,7 @@ jobs:
           node-version: "24"
           cache: npm
           cache-dependency-path: web/package-lock.json
-      # `make web` is `npm ci` + svelte-check + vite build, so a type error in
+      # `make web` is `npm ci` + svelte-check + vite build + vitest, so a type error in
       # the UI fails here too rather than shipping in a committed bundle.
       - name: Rebuild the console
         run: make web
